# 独立书库

此入口不会显示在普通书库、分类选项、命令候选或应用内帮助中。

- 在命令行完整输入 `vault`，或按 Ctrl+Shift+P。旧的 `/private` 写法仍可使用。
- 第一次进入设置至少 8 个字符的密码并确认；之后每次进入都必须验证密码。
- 进入后使用同样的 open、folder、ls、cat、分类、书签和最近阅读操作。提示符切到 Archive，普通书库为 Books。
- 两个书库独立保存书籍、分类、阅读位置、最近阅读、书签和命令历史；普通书库不会列出独立书库的条目或入口。
- `lock`、Ctrl+Shift+P、Ctrl+C 回主页、老板键、离开窗口都会锁定。老板键触发后继续保持程序输出；再次按下只返回普通主页，重新进入仍需密码。
- 将普通书籍移入：先 ls 并选择书籍（可多选），再完整输入 `vault move`；也可用 `vault move "书名"`。验证密码后提交转移，普通缓存随同加密写入在一个事务里删除，磁盘源文件保留。
- 旧版“私密”分类会在首次设置密码时迁入；失败不删除原缓存。
- 独立书库的 backup 始终加密；恢复需要密码，并且只能恢复到独立书库。普通书库的 backup 默认只包含普通书籍，可用 backup --encrypt 加密。

书库缓存及阅读记录使用 AES-256-GCM 加密，密码经 PBKDF2-SHA256（310000 次）派生密钥；每次写入使用新随机 IV。密码不写入命令历史或明文存储，锁定会清除内存中的解密状态。原始 TXT / EPUB 文件保持原样。没有绕过密码的恢复入口，请保管密码和备份。
