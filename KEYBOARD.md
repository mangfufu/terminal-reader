# 键盘操作

适用于 0.6.0，Windows 与 Linux 共用一套操作。文字仍可点击，纯文本界面保持不变。应用内随时按 **F1** 查看速查；再次按 F1 或 Esc 返回原控件，保留正在输入的内容。

速查打开期间自动阅读暂停，关闭后按原状态继续；输入、选中文字及其他面板也会让自动阅读让出操作。

## 统一规则

| 按键 | 行为 |
| --- | --- |
| Tab / Shift+Tab | 前后切换控件；面板内循环，不跳到被遮挡的正文 |
| F6 / Shift+F6 | 前后切换区域；阅读时在正文、命令行、标题栏之间切换；面板内切换筛选、操作、列表等区域 |
| ↑ ↓、Home End、PageUp PageDown | 列表内移动当前行；阅读时滚动正文 |
| ← → | 阅读时切换文件；分类、书签列表内切换当前行的操作 |
| Enter / 空格 | 执行当前文字操作；书库多选时空格勾选，Enter 打开当前书 |
| Esc | 先取消名称编辑，再退出子界面或多选，最后关闭面板回到正文 |
| F1 | 键盘速查，关闭后恢复原焦点 |

分类和书签列表只占一个 Tab 位置，用上下键换条目、左右键换操作。输入框保留普通文字编辑：Ctrl+A 选中文字，Ctrl+Z 撤销文字，Delete 删除字符。中文输入法确认候选字时不会触发打开或保存。

## 随时可用的入口

| 按键 | 入口 |
| --- | --- |
| Ctrl+K | 命令输入，从任意输入框也可使用 |
| / | 不在输入框中时打开命令输入 |
| Ctrl+O / Ctrl+Shift+O | 文件 / 文件夹选择 |
| Ctrl+B | 书库 |
| Ctrl+J | 章节 |
| Ctrl+D | 书签，默认选中建议名称，可直接输入新名称 |
| Ctrl+, | 外观与阅读设置 |
| Ctrl+F | 书库和章节内筛选；其他界面搜索正文 |
| Ctrl+Shift+F | 直接搜索正文 |
| Ctrl+Shift+B / Ctrl+Shift+R | 导出 / 恢复备份 |
| F10 | 终端外观与菜单，方向键选择、Enter 执行、Esc 退出 |

文件与备份使用应用内终端面板：Ctrl+L 输入路径，方向键选文件，Enter 进入目录或打开文件，Ctrl+Enter 确认，Esc 取消。窗口可通过 F6 到标题栏后用 Tab 选择控制项，也可用系统 Alt+F4 关闭。

## 书库与分类

| 按键 | 行为 |
| --- | --- |
| Ctrl+F → 输入 → ↓ | 筛选并回到书籍列表；输入框内 Esc 清空筛选并返回列表 |
| Ctrl+M | 进入 / 退出多选 |
| 空格 | 多选时勾选当前行 |
| Shift+↑/↓、Shift+Home/End | 范围选择，可往回缩小范围 |
| Ctrl+A | 全选当前筛选结果，必要时自动进入多选；保留其他筛选下已勾选的书 |
| Ctrl+Shift+A | 清空全部勾选 |
| Delete | 仅在书籍列表中：单选移出当前行，多选移出已勾选书籍 |
| Ctrl+Z | 撤销上一批移出，输入框中仍为文字撤销 |
| Ctrl+G | 聚焦分类筛选，用方向键选择 |
| Ctrl+Shift+G | 管理分类 |
| Alt+G → ↑/↓ → Ctrl+Enter | 选择目标分类并应用于当前行或已勾选书籍 |
| Esc | 输入筛选时先清空关键词；多选时退出多选；再按关闭书库 |

分类管理中：Ctrl+N 聚焦新分类名称，输入后 Enter 创建；F6 切到分类列表；F2 改名、Enter 保存、Esc 取消；Delete 删除当前分类。删除分类只把其中书籍移到未分类。Esc 从分类管理回到书库。

例：**Ctrl+B → Ctrl+A → Delete → Ctrl+Z**，全选移出后整批撤销。磁盘原文件、阅读位置和书签保留。

例：**Ctrl+B → Ctrl+Shift+G → Ctrl+N → 输入名称 → Enter → Esc → Ctrl+A → Alt+G → End → Ctrl+Enter**，新建分类并将所有书籍归入刚创建的分类。

## 阅读、搜索与书签

阅读时空格开始/暂停自动滚动；←/→ 切换文件，有历史就恢复位置，未读文件从首行开始。F3 / Shift+F3 切换搜索结果；Esc 结束搜索并回原位置。Alt+← 返回章节、搜索或书签跳转前的位置。

书签面板中：Ctrl+N 聚焦名称，Enter 新建并选中新书签；上下键切换书签，Enter 跳转；F2 改名，Enter 保存、Esc 取消；Delete 删除当前书签。删除和保存后焦点留在相邻或原条目，不需要点击找回焦点。

## 命令与设置

命令用 ↑/↓ 选择、Tab 补全、Enter 执行；Shift+Tab 离开输入框。空命令行 Tab 不补全，Enter 或 Esc 回到阅读。空命令行 ↑ 调出历史，也可用 Alt+↑/↓ 查看历史。

设置用 Tab 或 F6 到达控件，选择框、数值框和滑杆使用方向键；可直接输入字体和数值，离开控件时应用。自定义配色的折叠项用 Enter/空格展开，每种颜色都有 `#RRGGBB` 输入框：Enter 保存，Esc 恢复当前颜色值。颜色直接用终端文本输入，不再打开系统取色器。方案名称输入后 Enter 保存配色。

鼠标与键盘随时可以混用。操作过程中删除或重新绘制列表时，键盘焦点会留在可用条目上；没有条目时转到撤销或新建入口。

## 终端命令与快速上手

直接输入命令并按 Enter，保留 /open 等旧写法。阅读界面直接输入英文字母会进入命令行。

| 命令 | 操作 |
| --- | --- |
| open / folder | 终端面板导入文件 / 文件夹 |
| ls / dir / Get-ChildItem | 列出书库，显示可用于打开的编号 |
| cat 1 / type "书名.txt" / Get-Content "书名.txt" | 按当前列表编号或书名阅读 |
| cd "分类名" / cd .. / pwd | 进入分类 / 返回全部 / 查看分类 |
| recent / resume | 最近阅读和上次位置 / 接着读 |
| goto 35% / progress | 按文字位置跳转 / 全书、本章进度和预计剩余页数 |
| history / Ctrl+R | 搜索命令历史，Enter 选入命令行，再 Enter 执行 |
| clear / cls / Ctrl+L | 清屏回主页和命令行，保留书籍及进度 |
| backup / backup --encrypt / restore | 当前书库备份 / 设置密码加密备份 / 恢复 |
| help / guide | 快速上手 |

命令行内 Ctrl+U 清除光标前输入，Ctrl+W 删除前一个词，Ctrl+E 到行尾，Ctrl+P / Ctrl+N 浏览历史；上下键与 Tab 仍可使用。cat、type、open、cd 的参数支持 Tab 补全；包含空格的书名和分类用引号包住。

Alt+Q 是默认全局老板键。设置中可录入其他组合键；若被系统占用，会保留原可用按键并提示原因。窗口不在前台时触发只会遮挡，不会把原内容显示到前台。设置中可开启失去焦点自动伪装。

底部进度可点击轮换进度条、页数、百分比。设置中选择全书或当前章节；页数与剩余页数按窗口高度估算。goto 以文字锚点定位，调整字号后可继续保持位置。

文件选择：Ctrl+L 路径、Ctrl+F 筛选、↑/↓ 移动、Enter 进入/打开、空格勾选、Ctrl+A 全选文件、Alt+↑ 上级、Ctrl+Enter 确定；保存时 Ctrl+N 填写文件名。Esc 取消，Tab / F6 切区域，所有步骤在终端面板内完成。
