# Terminal Reader 0.6.0

默认 Alt+Q 已成为系统全局老板键；切到其他应用后也能遮挡阅读器。支持改键、冲突回退和可选失焦自动伪装。

独立书库不再是普通分类。普通界面没有其入口、条目或历史；首次设置密码，每次进入验证密码，离开、老板键或回主页后锁定。书籍与记录加密保存，旧私密分类通过事务迁移，备份在两个书库之间隔离。

终端支持无斜杠命令、PowerShell/CMD/Linux 常用别名、带空格路径、参数补全和命令行编辑快捷键；help/guide 提供上手说明。新增 recent/resume、全书/章节进度及预计剩余页数、goto 百分比跳转。

保留鼠标、原有键盘操作及终端文件选择。原始电子书文件不作修改。

升级会保留普通书库，并把 IndexedDB 升至版本 2；旧私密分类在首次设密时迁入加密存储。后续请使用新版读取升级后的书库。

操作说明：KEYBOARD.md；独立书库入口及密码、迁移、备份说明：PRIVATE-LIBRARY.md。

验收：34 项前端单元测试、12 项 Rust 测试通过；浏览器覆盖密码重试、迁移失败回滚、锁定与旧列表隔离、备份范围、命令补全和进度跳转。Windows 发布程序验证全局改键、失焦锁定、私密退出保存、分类多选、原生 EPUB、备份、单实例和 1/1.5/2/3 倍 WebView 缩放。Linux 发布程序在隔离 XDG、Xvfb/Openbox 环境验证密码、错误密码拒绝、加密备份解密后的书籍范围、EPUB、终端选择、280×160 小窗、单实例和正常关闭。
